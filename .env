# Remove or move these variables to individual .env files
# Puppeteer configuration
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false
PUPPETEER_EXECUTABLE_PATH=null

# Allow larger payloads for URL conversion
MAX_PAYLOAD_SIZE=50mb

# Increase timeout for URL conversion
URL_CONVERSION_TIMEOUT=30000